rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For development in GravityClaw, allow all local access.
      allow read, write: if true;
    }
  }
}
